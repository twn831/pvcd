name = "private-cloud-drive"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".next"

# Cloudflare Pages configuration
[vars]
NODE_VERSION = "20"

# Environment variables available in the dashboard
# PASSWORD_1, PASSWORD_2, PASSWORD_3, SESSION_SECRET
# R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_PUBLIC_DOMAIN
# D1_DATABASE_ID, STORAGE_LIMIT

# Durable Objects for session management (if needed)
# [[durable_objects.bindings]]
# name = "SESSIONS"
# class_name = "SessionManager"
